PDFQuery-LangChain

A simple PDF question-answering system built with LangChain, OpenAI embeddings, and FAISS.

Features

Load and parse PDFs

Split text into chunks

Generate embeddings

Retrieve relevant content

Answer questions using an LLM

Usage

Install dependencies

Set OPENAI_API_KEY

Run the Jupyter notebook and query your PDFs
